// function printMyName(){

// }

const printMyName = (name) => {
    console.log(name);
}

const multiply = number => number * 2; //this can only be used if the only thing in the
                                       //function's body is a return
printMyName('Pedro');

console.log(multiply(2));